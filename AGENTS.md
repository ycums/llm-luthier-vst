# 作業ポリシー

このファイルは、本リポジトリで作業するすべてのエージェントが**作業開始前に読む**。ここに書かれたルールは、個別のタスク指示より優先される。

矛盾する指示を受けた場合は、作業を止めて矛盾を報告すること。勝手にどちらかを選ばない。

---

## 1. 最重要の制約

**あなたは音を聴けない。**

したがって「音が良くなった」「自然になった」「近づいた」という主張は、このリポジトリでは**証拠として一切受け付けない**。音に関するすべての判断は、レンダ結果に対する数値指標（`docs/04-metrics.md`）を経由しなければならない。

この制約に例外はない。指標が未整備な領域については、判断を下すのではなく「指標がないため判断不能」と報告すること。

---

## 2. Definition of Done

以下をすべて満たしたときのみ、タスクを完了と呼んでよい。

- [ ] 変更が意図した範囲に収まっている（無関係なリファクタが混入していない）
- [ ] 実装は第3節のTDD手順（Red→Green→Refactor）に従った。従えなかった場合はその理由をPR本文に明記している
- [ ] テストが通っている。かつ、テストを通すためにテストを書き換えていない
- [ ] 音に影響する変更の場合、レンダ結果の指標差分がPRに添付されている
- [ ] 仕様（`docs/02` `docs/03`）を変更した場合、対応するADRが `docs/adr/` に追加されている
- [ ] 作業中に判明した未確定事項が `docs/06-open-questions.md` に追記されている

一つでも欠けている場合は「完了」ではなく「ここまで進んだ、残りはこれ」と報告する。

---

## 3. 実装手順（TDD: Red → Green → Refactor）

**コードを書いてから、それに合わせてテストを直す・書く**進め方は禁止する。この順序では、テストが「壊れた実装を検出できる」ことが一度も確認されないまま合格するため、テストが実装の追認にしかならない。

実装は必ず次の順序で進める。

1. **Red**: 仕様（Issue、`docs/02` `docs/03`、完了条件）から期待する入出力を先に定め、テストを書く。テストを実行し、**意図した理由で失敗する**ことを確認する（タイプミスや別のエラーで失敗していないかを見る）
2. **Green**: そのテストを通す最小限の実装を書く。テストの期待値・アサーションを、実装が実際に出した値に合わせて後から変更しない
3. **Refactor**: テストが通った状態を保ったまま、重複や可読性を整理する。この段階でテストの内容を変えてはならない。新しい仕様が必要だと分かった場合は、Refactorを続けず1.のRedに戻ってやり直す

**例外（スパイク）**: 期待する挙動が事前に分からず、先にテストを書けない場合（探索的デバッグ、既存動作の調査）は、使い捨てのコードで挙動を確認したあと、**そのコードは消してRedから書き直す**。スパイクのコードをそのまま清書し、テストを後付けする進め方は認めない。

PR本文の「どう検証したか」に、Red→Green→Refactorの各段階を経たことを書く。何らかの理由でテストファーストが不可能だった場合は、その理由を省略せずPR本文に明記する。

---

## 4. エビデンス要件

### 音に影響する変更（DSP、パラメータ写像、プリセット解釈）

PRに以下を必ず添付する：

1. ゴールデン音源セットに対するレンダ実行の**指標ベクトル差分**（変更前 / 変更後 / 差）
2. 差分が最大だった1〜2音源のスペクトログラム画像（変更前後の対）。生成手順は `harness/README.md`「スペクトログラム画像の生成」を参照
3. 悪化した指標があればその明示。「総合的には改善」という言い方で悪化を隠さない

指標が悪化しているのに改善と主張することは、このリポジトリで最も重い違反である。

### 音に影響しない変更（ビルド、ドキュメント、リファクタ）

指標添付は不要。ただし「音に影響しない」という判断の根拠を1行書くこと。

---

## 5. レビュー単位の上限

PRは以下を超えてはならない。超える場合は分割する。

| 項目 | 上限 |
|---|---|
| 責務 | 1つ |
| 変更ファイル数 | 10 |
| 変更行数（テスト・生成物を除く） | 400 |
| 仕様変更を含むPRでの実装変更 | 不可（仕様PRと実装PRを分ける） |

分割の切り方に迷った場合は、**「レビュアがこのPRだけを見て正しさを判定できるか」** を基準にする。判定に他のPRの知識が要るなら、切り方が間違っている。

---

## 6. 禁止事項

- 実装コードを先に書き、テストの期待値をその出力に合わせて決める（第3節のRed→Green→Refactorに反する）
- テストを通すためにテストを書き換える、閾値を緩める、テストをskipする
- 指標が悪化した状態でマージを提案する
- 未確定事項を推測で埋めて実装を進める（`docs/06-open-questions.md` に積んで止まる）
- 1音源だけを見てエンジン仕様を改訂する（コーパス単位で判断する。`docs/01-architecture.md` 参照）
- 実装より先にCIチェック項目やスキルを書き起こす（第8節）
- 外部ライブラリの新規追加（提案は可。追加はADRと承認を経る）

---

## 7. 未確定事項の扱い

仕様に書かれていないことに遭遇したとき、**それらしい答えを埋めない**。

1. 作業を止める
2. `docs/06-open-questions.md` に、何が不明で、何が決まれば進めるかを書く
3. 暫定判断で進める場合は、その判断をコード上のコメントではなくPR本文に明記する

「たぶんこうだろう」で埋めた仮定は、後で誰も見つけられない。

---

## 8. スキル化・ハーネス改善のルール

**先回りして環境を整えない。** サブエージェントの分割境界、CIチェック項目、スキルの中身は、実際に何が壊れるかを観測してからでないと正しく決められない。

獲得ルール：

- **3回ルール**：同じ種類の失敗・同じ手戻りが3回起きたら、その時点で初めてスキル化またはCIチェック追加を提案する
- 提案は `docs/log/session-log.md` の該当エントリを根拠として引用すること。根拠のない「あったら便利そう」は却下される
- メタ作業（環境整備）と対象作業（エンジン実装）を同一PRに混ぜない

---

## 9. セルフレビューのプロトコル

実装したエージェントとは別コンテキストのエージェントがレビューを行う。

**レビュア に渡すもの**
- 変更差分
- 関連する仕様ドキュメント
- CI出力・指標差分

**レビュア に渡さないもの**
- 実装時の会話履歴
- 実装者の意図説明

履歴を渡すと、レビュアは実装者の推論をなぞるだけになり、見落としが再生産される。照合先（仕様と数値）だけを渡すこと。

レビュアは、指摘が思いつかない場合に**指摘を捏造してはならない**。「仕様と差分の範囲では問題を検出できなかった」と報告するのが正しい振る舞いである。

---

## 10. コミットとPR

- コミットメッセージは日本語で可。1行目は変更の要約、本文に理由を書く
- PR本文には必ず「何を変えたか」ではなく「なぜ変えたか」と「どう検証したか」を書く。差分を読めばわかることを繰り返さない
- Issueと1対1で対応させる。対応するIssueがない作業は、まずIssueを立てる
- PR本文には対応Issueを**GitHubのクローズキーワード**（`Closes #<番号>` / `Fixes #<番号>` / `Resolves #<番号>`）で記載する。「対応Issue: #12」のような書き方はマージしてもIssueが自動クローズされないため禁止する。テンプレートの `Closes #` 欄をそのまま使うこと

---

## 11. 用語

`docs/05-glossary.md` の用語を使う。特に「残差」「能力ギャップ」「フィット誤差」「マクロ軸」は定義が厳密に決まっているので、日常語として流用しないこと。

---

## 12. ローカル Windows/MSVC ビルドの確定レシピ

このリポジトリは Windows/MSVC（`vcvars64.bat` 環境）をサポート対象に含む（`docs/adr/0008`）。**ローカルの C++ ビルド・テストを CLI（git-bash / MSYS）から起動するときは、この節の手順で行うこと。** 別の方法（下記「ダメな例」）は MSYS のパス変換でコマンドが実行されず、環境要因でトークンと時間を無駄にする（`docs/log/session-log.md` S-005）。

### ステップ1: ビルド一式を `.bat` に書く

`.bat` は Windows ネイティブのテキスト（CRLF）で書く。worktree内のどこでもよい（例: `<repo>/.worktrees/<name>/engine/build_verify.bat`）。

```bat
@echo off
call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\VC\Auxiliary\Build\vcvars64.bat"
cd /d "C:\フルパス\...\engine"
cmake -S . -B build -G Ninja
cmake --build build
ctest --test-dir build --output-on-failure
echo BUILD_RC=%ERRORLEVEL%
```

### ステップ2: bash から実行する（この変数2つが鍵）

```bash
MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*' cmd.exe /c "C:\\フルパス\\...\\engine\\build_verify.bat"
```

- **`MSYS_NO_PATHCONV=1` と `MSYS2_ARG_CONV_EXCL='*'` を必ず指定する**。これが無いと、`cmd.exe /c` に渡す引数が MSYS のパス変換で壊れ、コマンドが実行されない
- `build_verify.bat` のフルパスはネイティブ Windows 形式（`C:\...\`）。環境変数の問題を避けるため `.bat` 内の `call` も巻き込まれないよう、リポジトリはそのまま

### ダメな例（MSYS で壊れる）

- `cmd //c "call vcvars64.bat && cmake --build ..."` — `//c` がネイティブ `cmd /c` にならず**インタラクティブな cmd プロンプトが起動して止まる**
- `cmd.exe //c "....bat"` — 同様に壊れる
- コマンド内に複合 `&&` / `|` や長いワンライナーを詰め込む — Hermes の `terminal` ではハードライン制限（パース不能 = ブロックリスト）に当たり、実行すらされない。**コマンドを分解して1つずつ実行する**

### pytest 実行（ハーネス）

Python 側（`tests/`、`harness/`）は `pytest` をリポジトリの venv で実行する。**`LUTHIER_RENDER_BIN` 環境変数は必ず「現在作業している worktree の」CLI バイナリを指すように設定すること。** 別の worktree のバイナリを指したままだと、ビルドが通ったのに pytest が偽の結果になる（過去に発生。P1-10）。worktree（`.worktrees/<name>/`）をまたぐ作業では環境変数を毎回明示的に設定し直す。

### 従属注意: stacked PR / 複数worktree

- 複数 worktree（`.worktrees/`）を並行使用するとき、それぞれに独立した `engine/build/` を持つ。ビルド毎に自分がいる worktree のパスを確認してから実行すること
- GitHub の stacked PR で API からマージする場合、`gh pr merge` は拒否され、`PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge-async`（非同期マージAPI）を使う（詳細は GitHub docs「Merge a pull request asynchronously」）。最上位の PR のマージがスタック全体をマージする
