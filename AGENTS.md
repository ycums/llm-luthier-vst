# 作業ポリシー

このファイルは、本リポジトリで作業するすべてのエージェントが**作業開始前に読む**。ここに書かれたルールは、個別のタスク指示より優先される。

矛盾する指示を受けた場合は、作業を止めて矛盾を報告すること。勝手にどちらかを選ばない。

---

## 1. 最重要の制約

**あなたは音を聴けない。**

したがって「音が良くなった」「自然になった」「近づいた」という主張は、このリポジトリでは**証拠として一切受け付けない**。音に関するすべての判断は、レンダ結果に対する数値指標（`docs/04-metrics.md`）を経由しなければならない。

この制約に例外はない。指標が未整備な領域については、判断を下すのではなく「指標がないため判断不能」と報告すること。

---

## 2. Definition of Done

以下をすべて満たしたときのみ、タスクを完了と呼んでよい。

- [ ] 変更が意図した範囲に収まっている（無関係なリファクタが混入していない）
- [ ] テストが通っている。かつ、テストを通すためにテストを書き換えていない
- [ ] 音に影響する変更の場合、レンダ結果の指標差分がPRに添付されている
- [ ] 仕様（`docs/02` `docs/03`）を変更した場合、対応するADRが `docs/adr/` に追加されている
- [ ] 作業中に判明した未確定事項が `docs/06-open-questions.md` に追記されている

一つでも欠けている場合は「完了」ではなく「ここまで進んだ、残りはこれ」と報告する。

---

## 3. エビデンス要件

### 音に影響する変更（DSP、パラメータ写像、プリセット解釈）

PRに以下を必ず添付する：

1. ゴールデン音源セットに対するレンダ実行の**指標ベクトル差分**（変更前 / 変更後 / 差）
2. 差分が最大だった1〜2音源のスペクトログラム画像（変更前後の対）
3. 悪化した指標があればその明示。「総合的には改善」という言い方で悪化を隠さない

指標が悪化しているのに改善と主張することは、このリポジトリで最も重い違反である。

### 音に影響しない変更（ビルド、ドキュメント、リファクタ）

指標添付は不要。ただし「音に影響しない」という判断の根拠を1行書くこと。

---

## 4. レビュー単位の上限

PRは以下を超えてはならない。超える場合は分割する。

| 項目 | 上限 |
|---|---|
| 責務 | 1つ |
| 変更ファイル数 | 10 |
| 変更行数（テスト・生成物を除く） | 400 |
| 仕様変更を含むPRでの実装変更 | 不可（仕様PRと実装PRを分ける） |

分割の切り方に迷った場合は、**「レビュアがこのPRだけを見て正しさを判定できるか」** を基準にする。判定に他のPRの知識が要るなら、切り方が間違っている。

---

## 5. 禁止事項

- テストを通すためにテストを書き換える、閾値を緩める、テストをskipする
- 指標が悪化した状態でマージを提案する
- 未確定事項を推測で埋めて実装を進める（`docs/06-open-questions.md` に積んで止まる）
- 1音源だけを見てエンジン仕様を改訂する（コーパス単位で判断する。`docs/01-architecture.md` 参照）
- 実装より先にCIチェック項目やスキルを書き起こす（第7節）
- 外部ライブラリの新規追加（提案は可。追加はADRと承認を経る）

---

## 6. 未確定事項の扱い

仕様に書かれていないことに遭遇したとき、**それらしい答えを埋めない**。

1. 作業を止める
2. `docs/06-open-questions.md` に、何が不明で、何が決まれば進めるかを書く
3. 暫定判断で進める場合は、その判断をコード上のコメントではなくPR本文に明記する

「たぶんこうだろう」で埋めた仮定は、後で誰も見つけられない。

---

## 7. スキル化・ハーネス改善のルール

**先回りして環境を整えない。** サブエージェントの分割境界、CIチェック項目、スキルの中身は、実際に何が壊れるかを観測してからでないと正しく決められない。

獲得ルール：

- **3回ルール**：同じ種類の失敗・同じ手戻りが3回起きたら、その時点で初めてスキル化またはCIチェック追加を提案する
- 提案は `docs/log/session-log.md` の該当エントリを根拠として引用すること。根拠のない「あったら便利そう」は却下される
- メタ作業（環境整備）と対象作業（エンジン実装）を同一PRに混ぜない

---

## 8. セルフレビューのプロトコル

実装したエージェントとは別コンテキストのエージェントがレビューを行う。

**レビュア に渡すもの**
- 変更差分
- 関連する仕様ドキュメント
- CI出力・指標差分

**レビュア に渡さないもの**
- 実装時の会話履歴
- 実装者の意図説明

履歴を渡すと、レビュアは実装者の推論をなぞるだけになり、見落としが再生産される。照合先（仕様と数値）だけを渡すこと。

レビュアは、指摘が思いつかない場合に**指摘を捏造してはならない**。「仕様と差分の範囲では問題を検出できなかった」と報告するのが正しい振る舞いである。

---

## 9. コミットとPR

- コミットメッセージは日本語で可。1行目は変更の要約、本文に理由を書く
- PR本文には必ず「何を変えたか」ではなく「なぜ変えたか」と「どう検証したか」を書く。差分を読めばわかることを繰り返さない
- Issueと1対1で対応させる。対応するIssueがない作業は、まずIssueを立てる

---

## 10. 用語

`docs/05-glossary.md` の用語を使う。特に「残差」「能力ギャップ」「フィット誤差」「マクロ軸」は定義が厳密に決まっているので、日常語として流用しないこと。
