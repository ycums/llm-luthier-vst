---
name: nop
description: セッションIDを1行だけ出力し、他には何もしない。親セッションが子セッションのIDを把握するための軽量なハンドシェイクに使う。対になるスキルは delegate-subtasks。
---

# nop

このSkillの唯一の仕事は、自分自身のセッションIDを出力することであり、
それ以外は一切行わない。

## 手順

1. `mcp__ccd_session_mgmt__get_session` を `session_id: "self"` で呼び、
   返り値の `sessionId` フィールドの値を取得する。
2. 取得した値**のみ**を、前置き・説明・挨拶・コードブロック・その他の
   装飾を一切付けずに1行で出力する。
3. これ以外のツール呼び出しは行わない。ファイルの読み書き、調査、
   他セッションへのメッセージ送信なども一切しない。
